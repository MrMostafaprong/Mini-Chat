# سجل الإصلاحات — MiniChat

هذا الملف يوثق كل الإصلاحات التي أُجريت على المشروع الأصلي، مقسّمة حسب الفئة.

## 🔒 ثغرات أمنية (تم إصلاحها)

1. **IDOR في `socket.on("read-messages")`** — كان بإمكان أي مستخدم متصل تمييز
   رسائل أي محادثة كـ"مقروءة" بمجرد معرفة `conversationId`، حتى لو لم يكن
   عضواً فيها. تم إضافة تحقق من العضوية قبل التنفيذ.
2. **IDOR/Harassment في `socket.on("typing")`** — كان بإمكان أي مستخدم إرسال
   إشعار "يكتب..." وهمي لأي مستخدم آخر بمجرد معرفة الـ ID، دون وجود محادثة
   مشتركة. تم إضافة تحقق من عضوية الطرفين في نفس المحادثة.
3. **Rate limiting ناقص على `/api/users/search`** — كان يعتمد فقط على الحد
   العام المشترك (300 طلب/15 دقيقة)، مما يسمح بتعداد أسماء المستخدمين
   بسرعة. أُضيف حد مخصوص (20 بحث/دقيقة لكل مستخدم).
4. **`trust proxy` غير مُفعّل** — دون هذا الإعداد خلف reverse proxy، يصبح
   express-rate-limit غير فعّال بالكامل (كل الطلبات تظهر بنفس IP). أُضيف
   كـ opt-in عبر متغير البيئة `TRUST_PROXY`.
5. **لا حد أقصى لعدد الرسائل المُرجعة (`GET /conversations/:id/messages`)** —
   كان يُرجع كل السجل التاريخي دفعة واحدة، مما يشكّل خطر أداء/DoS مع
   محادثات طويلة. أُضيف pagination (50 رسالة افتراضياً، مع `before` cursor
   لتحميل الأقدم).

## 🗄️ قاعدة البيانات

- أُعيدت كتابة `database/schema.sql` بالكامل لتطابق الاستعلامات الفعلية في
  الكود (كان الملف الأصلي ناقصاً: `conversation_members`, `login_attempts`,
  أعمدة `avatar`/`token_version` في `users`، وأعمدة `type`/`name`/`avatar`/
  `created_by` في `conversations`).
- رُفع حد طول `messages.content` من 5000 إلى 7,000,000 حرف (كان سيرفض أي
  صورة/ملف base64 حقيقي عند الإدراج).

## 🧹 كود ميت

- حُذف `server/src/services/authService.js` و`messageService.js` — لم
  يكونا مستخدمين في أي مسار فعلي، ومنطقهما كان أقدم/أضعف من النسخة الحقيقية
  المستخدمة في `routes/`.

## 🐞 أخطاء وظيفية

- `POST /auth/logout` لم يكن يمرّ عبر أي middleware مصادقة، فكان يسجّل
  `userId: undefined` دائماً. أُضيف `optionalAuthenticate`.
- `Navbar.jsx` كان ينادي `/auth/logout` مرتين (مرة بنفسه، ومرة عبر
  `onLogout` الممرَّرة من `Chat.jsx`).
- نتائج `/users/search` كانت تُعرض بحقل `email` في الواجهة رغم أن الـ API لا
  يُرجعه عمداً (خصوصية) — كان يظهر فارغاً دائماً. أُزيل من العرض.
- التحقق من نوع محتوى الرسائل (`image`/`file`) كان يعتمد على طول النص فقط
  دون فحص فعلي لنوع المحتوى (على عكس رفع الصورة الشخصية). أُضيف فحص
  `data:` prefix مطابق لأنواع الملفات المسموحة فعلياً في الواجهة.
- `Login.jsx`/`Register.jsx` كانا يستخدمان `.trim()` على كل ضغطة مفتاح بما
  في ذلك حقل كلمة المرور، مما يمنع كتابة مسافات داخلها أثناء الإدخال.
- Socket الكلاينت كان يُغلق ويُعاد فتحه بالكامل عند كل تبديل محادثة (بسبب
  اعتماد الـ effect على `activeConversation.id`)، مما يفقد حالة الاتصال
  والغرف بلا داعٍ. أُصلح باستخدام `ref` بدلاً من الاعتماد المباشر.
- `reconnectionAttempts` في الكلاينت كان محدوداً بـ 5 محاولات، فيتوقف
  نهائياً عن إعادة الاتصال إذا طال الانقطاع. أصبح `Infinity` مع إشعار بصري
  لحالة الاتصال (متصل/منقطع/يعيد المحاولة).
- الرسائل الجديدة كانت تُبث فقط عبر حدث socket منفصل يرسله الكلاينت بعد
  نجاح REST call، فإن انقطع الاتصال بين الخطوتين تُفقد الرسالة من ناحية
  البث الفوري رغم حفظها. أصبح السيرفر يبث `new-message` مباشرة من
  `routes/messages.js` بعد نجاح الحفظ.
- `Login.jsx`: `id` الخاص بحقل كلمة المرور (`login-password`) لم يطابق
  `htmlFor` في الـ `<label>` المقابل (accessibility bug).
- رفع الملفات في `MessageInput.jsx` لم يكن يتحقق من حجم الملف قبل قراءته
  بالكامل عبر `FileReader`، مما قد يُجمّد المتصفح على ملفات ضخمة سيرفضها
  الخادم على أي حال. أُضيف تحقق من الحجم (5MB) قبل القراءة.
- `new-message` على الكلاينت كان يستدعي `fetchConversations()` (طلب API
  كامل) عند كل رسالة واردة بغض النظر عن المحادثة. أصبح يُحدّث `lastMessage`
  و`unreadCount` محلياً، مع اللجوء لـ `fetchConversations()` فقط عند وصول
  رسالة من محادثة غير موجودة أصلاً في القائمة.

## ✅ تم التحقق منه ولا يحتاج تعديلاً

- كل استعلامات SQL معاملة (parametrized) بالكامل — لا ثغرات SQL injection.
- فحوصات الصلاحية (IDOR) في `conversations.js` و`groups.js` سليمة قبل كل
  عملية حساسة.
- `errorHandler.js`/`logger.js` لا يسرّبان stack traces أو بيانات حساسة.
- كلمات المرور: bcrypt cost 12، dummy hash لمنع timing attacks عند تسجيل
  الدخول، تحقق قوة كلمة مرور جيد.
